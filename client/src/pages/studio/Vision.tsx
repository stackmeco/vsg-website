import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function Vision() {
  return (
    <PlaceholderPage
      title="Vision"
      category="Studio"
      description="Where we're heading. A future where verification replaces trust and individuals control their own identity."
    />
  );
}
