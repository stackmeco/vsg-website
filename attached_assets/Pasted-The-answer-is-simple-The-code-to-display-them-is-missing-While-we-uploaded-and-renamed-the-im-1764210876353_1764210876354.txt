The answer is simple: **The code to display them is missing.**

While we uploaded and renamed the image files (the *Assets*), we haven't actually updated the **React Components** (the *Logic*) to render them on the screen. Right now, your `System.tsx` and `PipelineDetail.tsx` files are still just rendering plain text.

Here is the exact code to "wire up" the visuals so they appear as **Institutional Textures** (low opacity, grayscale) rather than distracting photos.

-----

### **1. Fix the System Page (The Gears)**

**Location:** `client/src/pages/System.tsx`
**Goal:** Add the "Gears" texture behind the "Bitcoin Treasury Program" section.

**Instructions:** Replace the **Imports** and the **Treasury Section** with this code.

```tsx
// 1. Add Import at top
import gearsTexture from "@assets/gears_texture.jpg";

// 2. Find the "Bitcoin Treasury Program" section and replace it with this:
<section className="py-20 relative overflow-hidden border-b border-border" data-testid="section-treasury">
  {/* BACKGROUND TEXTURE (The Gears) */}
  <div 
    className="absolute inset-0 z-0 opacity-10 pointer-events-none mix-blend-overlay"
    style={{
      backgroundImage: `url(${gearsTexture})`,
      backgroundSize: "cover",
      backgroundPosition: "center",
      filter: "grayscale(100%) contrast(125%)"
    }}
  />
  
  {/* CONTENT (Z-Index 10 to sit on top) */}
  <div className="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div className="max-w-3xl">
      <h2 className="font-heading font-bold text-3xl text-foreground mb-6">
        Bitcoin Treasury Program
      </h2>
      <p className="text-muted-foreground leading-relaxed mb-6">
        We hold BTC on our own balance sheet. When the risk/reward looks reasonable, we borrow stablecoins against a portion of that BTC...
      </p>
      {/* ... rest of your text content ... */}
    </div>
  </div>
</section>
```

-----

### **2. Fix the Pipeline Detail Pages (The Cube & Lens)**

**Location:** `client/src/pages/PipelineDetail.tsx`
**Goal:** Dynamically load the specific texture for **Axiom** and **Lumina**.

**Instructions:** This requires three small changes in the same file.

**A. Add Imports at the top:**

```tsx
import axiomTexture from "@assets/axiom_cube.jpg";
import luminaTexture from "@assets/lumina_lens.jpg";
import gridTexture from "@assets/grid_texture.jpg"; // Fallback for others
```

**B. Add an `image` field to your `projectData` object:**
*Update the data object at the top of the file:*

```tsx
const projectData: Record<string, ProjectData & { texture?: string }> = {
  axiom: {
    // ... keep existing fields ...
    texture: axiomTexture, // <--- NEW
  },
  lumina: {
    // ... keep existing fields ...
    texture: luminaTexture, // <--- NEW
  },
  // For others, we will use the fallback grid
};
```

**C. Update the Hero Section Render:**
*Find the `<section data-testid="section-hero">` and replace it with this:*

```tsx
<section className="relative py-20 lg:py-28 overflow-hidden border-b border-border" data-testid="section-hero">
  
  {/* DYNAMIC HEADER TEXTURE */}
  <div 
    className="absolute inset-0 z-0 opacity-20 pointer-events-none"
    style={{
      backgroundImage: `url(${project.texture || gridTexture})`, // Uses specific image or fallback grid
      backgroundSize: "cover",
      backgroundPosition: "center",
      filter: "grayscale(100%) contrast(120%)"
    }}
  />
  
  {/* FADE GRADIENT (To make text readable) */}
  <div className="absolute inset-0 z-10 bg-gradient-to-r from-background via-background/90 to-transparent" />

  {/* CONTENT */}
  <div className="relative z-20 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <Link href="/pipeline" className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground mb-8 transition-colors">
      <ArrowLeft className="w-4 h-4" />
      Back to Pipeline
    </Link>
    
    <div className="flex flex-wrap items-start justify-between gap-4 mb-6">
      <h1 className="font-heading font-bold text-4xl sm:text-5xl text-foreground">
        {project.name}
      </h1>
      <StageChip stage={project.stage} variant={project.stageVariant} />
    </div>
    
    <p className="text-xl text-primary font-mono mb-6 tracking-tight">
      {project.tagline}
    </p>
    
    <p className="text-muted-foreground leading-relaxed max-w-3xl">
      {project.description}
    </p>
  </div>
</section>
```

### **Why this solves it:**

1.  **Visibility:** By adding the code, the images will finally appear.
2.  **Consistency:** By using `grayscale(100%)` and `opacity-20`, we force these different images (Gears, Cube, Lens) to look like they belong to the same "Visual System." They become background radiation, not foreground decorations.